// 定义 TabBarClass 类，用于表示底部导航栏的一个选项卡
export class TabBarClass {
  // 选项卡的标题，默认为空字符串
  title: string = "";
  // 选项卡的名称，默认为空字符串
  name: string = "";
  // 选项卡的图标资源，类型为 ResourceStr，默认为空字符串
  icon: ResourceStr = "";
  // 选项卡选中时的图标资源，类型为 ResourceStr，默认为空字符串
  selectIcon: ResourceStr = "";
}